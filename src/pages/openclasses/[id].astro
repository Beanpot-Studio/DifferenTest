---
// open classes - all can view, few can take quiz

import Layout from "../../layouts/MainLayout.astro";
import FirebaseService from "../../lib/firebaseService";

export async function getStaticPaths() {
  // Get all public classes to generate paths
  const publicClasses = await FirebaseService.getPublicClasses();

  // Return paths for each class
  return publicClasses.map((classData) => ({
    params: { id: classData.id },
    props: { classData },
  }));
}

const { id } = Astro.params;
const { classData } = Astro.props;

if (!classData) {
  return Astro.redirect("/404");
}

---

<Layout title={classData.name}>
  <main class="container mx-auto px-4 py-8">
    <div class="bg-white rounded-lg shadow-md p-6">
      <h1 class="text-3xl font-bold text-gray-900 mb-4">{classData.name}</h1>
      <p class="text-gray-600 mb-6">{classData.description}</p>

      <div class="flex items-center text-sm text-gray-500 mb-8">
        <span class="mr-4">Author: {classData.teacherName}</span>
      </div>

      <!-- Quiz Navigation -->
      <div class="mb-8">
        <h2 class="text-2xl font-semibold text-gray-900 mb-6">Quizzes</h2>
        <div class="flex space-x-4 mb-6">
          {
            classData.quizzes.map((quiz) => (
              <a
                href={`/openclasses/${id}/quiz/${quiz.id}`}
                class="px-4 py-2 bg-gray-100 hover:bg-gray-200 rounded-lg text-gray-700"
              >
                {quiz.title}
              </a>
            ))
          }
        </div>
      </div>
    </div>
  </main>
</Layout>